Her skal jeg lage en generisk klasse som kan legge til og fjerne objekter.
Klassen skal implementere en generisk type
Så skal jeg lage et interface.
Klassen skal hete Storage
Grensesnittet(interface) skal hete IStore
la til legge til, velge og telle
La til sletting via navn, posisjon og slett alt inhold.
prøve å lage en liten del som kan brukes i et spill (pockets og keys)
